# Руководство по типам презентаций AI SlideRush

## Обзор функционала

В AI SlideRush теперь доступен выбор типа презентации для создания оптимальной структуры слайдов под конкретные цели.

## Доступные типы презентаций

### 1. 💼 Бизнес-презентация (Business Presentation)
**Когда использовать:** Корпоративные встречи, отчёты, стратегические планы

**Структура:**
- Введение (представление компании и темы)
- Проблема (описание вызова)
- Решение (предложение)
- Результаты (достижения и метрики)
- Призыв к действию (следующие шаги)

**Советы:** Фокус на фактах, данных и конкретных результатах. Используйте графики и диаграммы.

---

### 2. 💰 Продажи (Sales Pitch)
**Когда использовать:** Презентация продукта/услуги потенциальным клиентам

**Структура:**
- Hook (привлечение внимания с первых секунд)
- Проблема (болевые точки клиента)
- Решение (ваш продукт/услуга)
- Преимущества (уникальные особенности)
- Цена (ценовое предложение)
- Демонстрация (примеры и кейсы)
- Призыв к действию (закрытие сделки)

**Советы:** Эмоциональность + конкретика. Покажите ROI и быстрые победы.

---

### 3. 📈 Инвестиционный питч (Investor Pitch)
**Когда использовать:** Привлечение инвестиций, презентация стартапа

**Структура:**
- Проблема (глобальная проблема на рынке)
- Решение (инновационное решение)
- Рынок (размер и потенциал)
- Бизнес-модель (как зарабатываете)
- Команда (ключевые люди)
- Финансы (прогнозы и потребность в капитале)
- Призыв к действию (инвестиционное предложение)

**Советы:** Масштабируемость, рост, команда. Фокус на цифрах и потенциале.

---

### 4. 🎓 Образовательная (Educational)
**Когда использовать:** Лекции, тренинги, обучающие материалы

**Структура:**
- Введение (цели и обзор темы)
- Теория (основные концепции)
- Примеры (практические примеры)
- Практика (упражнения и задания)
- Выводы (ключевые моменты)
- Вопросы (Q&A и дополнительные материалы)

**Советы:** Ясность, структура, повторение. Используйте визуализацию и примеры.

---

## Использование в коде

### Frontend (JavaScript)

```javascript
// Получение типов презентаций через API
const response = await fetch('/api/presentation-types');
const data = await response.json();
console.log(data.types); // Объект с доступными типами

// Создание презентации с типом
const presentationType = 'investor'; // business, sales, investor, educational
const response = await fetch('/api/create-presentation', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({
        topic: 'My Startup',
        num_slides: 7,
        language: 'ru',
        theme: 'modern',
        presentation_type: presentationType
    })
});
```

### Backend (Python/Flask)

```python
from app import PRESENTATION_TYPES, validate_presentation_type, get_presentation_type_info

# Валидация типа
if validate_presentation_type('investor'):
    print('Тип валиден')

# Получение информации о типе
type_info = get_presentation_type_info('sales')
print(type_info['name_ru'])  # Продажи
print(type_info['icon'])     # 💰
print(type_info['color'])    # #27ae60
print(type_info['structure']) # Массив с структурой слайдов
print(type_info['tips'])     # Советы по созданию
```

### База данных

Таблица `presentations` содержит поле `presentation_type`:

```sql
SELECT id, topic, presentation_type, creation_date 
FROM presentations 
WHERE user_id = ?;
```

Фильтрация по типу:

```sql
SELECT * FROM presentations 
WHERE user_id = ? AND presentation_type = 'business'
ORDER BY creation_date DESC;
```

---

## Фильтрация в Dashboard

На странице `/dashboard` доступна фильтрация презентаций по типу:

- **URL параметры:**
  - `?type=business` - показать только бизнес-презентации
  - `?type=sales` - показать только продажи
  - `?search=keyword&type=investor` - комбинация поиска и фильтра

- **Визуальные метки:**
  - Каждая презентация имеет цветной бейдж с иконкой типа
  - Цвет соответствует типу (business: фиолетовый, sales: зелёный и т.д.)

---

## Миграция базы данных

При первом запуске автоматически выполняется миграция:

```python
# Проверка наличия поля presentation_type
cursor.execute("PRAGMA table_info(presentations)")
columns = [column[1] for column in cursor.fetchall()]

# Добавление поля если отсутствует
if 'presentation_type' not in columns:
    cursor.execute('ALTER TABLE presentations ADD COLUMN presentation_type TEXT DEFAULT "business"')
```

Для существующих презентаций по умолчанию установлен тип `"business"`.

---

## API Endpoints

### GET /api/presentation-types
Получить список всех доступных типов презентаций

**Response:**
```json
{
  "success": true,
  "types": {
    "business": {
      "name_ru": "Бизнес-презентация",
      "name_en": "Business Presentation",
      "icon": "💼",
      "color": "#667eea",
      "structure": [...],
      "tips": "..."
    },
    ...
  }
}
```

### POST /api/create-presentation
Создать презентацию с указанным типом

**Request:**
```json
{
  "topic": "AI in Healthcare",
  "num_slides": 7,
  "language": "en",
  "theme": "modern",
  "presentation_type": "investor"
}
```

**Response:**
```json
{
  "success": true,
  "filename": "AI_in_Healthcare_20231122.pptx",
  "slides": [...],
  "presentation_type": "investor"
}
```

---

## Best Practices

1. **Выбор правильного типа:**
   - Для стартапов → Investor Pitch
   - Для B2B продаж → Sales Pitch
   - Для внутренних отчётов → Business Presentation
   - Для обучения → Educational

2. **Адаптация контента:**
   - Каждый тип имеет свою структуру
   - AI генерирует контент с учётом выбранного типа
   - Количество слайдов может варьироваться (3-10)

3. **Дизайн и тема:**
   - Комбинируйте тип презентации с подходящей темой
   - Investor Pitch + Modern Theme
   - Educational + Light Theme
   - Sales Pitch + Vivid Theme

---

## Примеры использования

### Пример 1: Создание инвестиционного питча
```html
<select id="presentationType">
    <option value="investor">📈 Инвестиционный питч</option>
</select>
```

### Пример 2: Фильтрация в Dashboard
```python
@app.route('/dashboard')
def user_dashboard():
    filter_type = request.args.get('type', '').strip()
    
    if filter_type and validate_presentation_type(filter_type):
        # Фильтрация по типу
        presentations = get_presentations_by_type(user_id, filter_type)
    else:
        # Все презентации
        presentations = get_all_presentations(user_id)
```

### Пример 3: Отображение типа в списке
```html
{% for presentation in presentations %}
    <span class="type-badge" style="color: {{ presentation.type_info.color }}">
        {{ presentation.type_info.icon }} {{ presentation.type_info.name_ru }}
    </span>
    <strong>{{ presentation.topic }}</strong>
{% endfor %}
```

---

## Поддержка и расширение

Для добавления нового типа презентации:

1. Добавьте запись в `PRESENTATION_TYPES` в `app.py`:
```python
'custom_type': {
    'name_ru': 'Название',
    'name_en': 'Name',
    'icon': '🎯',
    'color': '#hexcolor',
    'structure': [...],
    'tips': 'Советы'
}
```

2. Добавьте опцию в HTML форму (`templates/index.html`)

3. Обновите CSS стили при необходимости

---

## Технические детали

- **База данных:** SQLite
- **ORM:** Нет (используется sqlite3 напрямую)
- **Frontend:** Vanilla JavaScript
- **Backend:** Python 3.x + Flask
- **Валидация:** Встроенная функция `validate_presentation_type()`
- **Цветовая схема:** Hex-коды для каждого типа

---

**Версия:** 1.0  
**Дата создания:** 2024  
**Автор:** AI SlideRush Team
